<rml>
  <head>
    <style>
      body {
        font-family: "OCR A Extended";
        background-color: transparent;
        margin: 0;
      }

      /* ─── Glass panel ───────────────────────────────────────── */
      #pause-container {
        width: 420px;
        padding: 32px 48px;
        display: flex;
        flex-direction: column;
        gap: 24px;
        align-items: stretch;
        justify-content: center;

        /* background image, scaled to fill  */
        decorator: image("../textures/invader.tga");

        /* dark tint over the image */
        background-color: rgba(0,0,0,0.55);

        /* cyan glow frame */
        border: 2px solid #00FFFF;
        border-radius: 18px;
        box-shadow:
          0 0 15px  rgba(0,255,255,0.55),
          0 0 60px  rgba(0,255,255,0.20) inset;

        position: absolute;
        left: 50%;
        top: 50%;
        transform: translate(-50%, -50%);
      }

      /* ─── Buttons ──────────────────────────────────────────── */
      button {
        width: 100%;
        padding: 14px 0;
        font-size: 22px;
        color: #FFFFFF;

        /* flat colours (RCSS gradients unsupported) */
        background-color: #0070FF;
        border: 2px solid #00FFFF;   /* solid cyan */
        border-radius: 12px;
        cursor: pointer;
      }

      #resume { background-color: #00AA66; border-color: #00FFAA; }
      #change { background-color: #FFC800; border-color: #FFD700; }
      #quit   { background-color: #CC3333; border-color: #FF5050; }
    </style>
  </head>

  <body>
    <div id="pause-container">
      <button id="resume">▶ Resume</button>
      <button id="change">🔄 Change&nbsp;Level</button>
      <button id="quit">❌ Quit</button>
    </div>
  </body>
</rml>
